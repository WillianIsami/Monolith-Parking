# Ambiente para servicos da aplicacao rodando dentro do Docker Compose.
# Use este modelo no backend/API/simulador quando eles estiverem no mesmo compose do banco.

DATABASE_URL=postgresql://parking:parking123@postgres:5432/monolith_parking
PGSSLMODE=disable
DB_APPLICATION_NAME=monolith-parking-service
DB_SCHEMA=public
